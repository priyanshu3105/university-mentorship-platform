VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_KEY

# Optional: Socket.IO server origin if it is not the same as this app (e.g. API on another subdomain).
# Leave unset in local dev — Vite proxies /socket.io to the backend on :4000.
# VITE_SOCKET_URL=https://api.example.com
